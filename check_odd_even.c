#include<stdio.h>
#include<conio.h>
int main()
{
    int num;
    printf("Please Enter the NO. to check : ");
    scanf("%d",&num);
    if (num%2==0)
    {
       printf("%d is Even Number\n",num);
    }
    else{ 
        printf("%d is Even Number\n",num); 
    }
    
 return 0;
}